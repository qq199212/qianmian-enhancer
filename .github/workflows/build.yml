name: Build Qianmian Enhancer

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    name: Build Tweak
    runs-on: macos-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Dependencies
        run: |
          brew install ldid xz gnu-tar
          echo "Installed dependencies"

      - name: Setup Theos
        run: |
          export THEOS=$HOME/theos
          git clone --recursive https://github.com/theos/theos.git $THEOS
          echo "THEOS=$THEOS" >> $GITHUB_ENV
          echo "$THEOS/bin" >> $GITHUB_PATH

      - name: Download iOS SDK
        run: |
          # 从 Theos 官方 SDK 仓库获取（更可靠）
          git clone --depth 1 https://github.com/theos/sdks.git $THEOS/sdks-repo
          mkdir -p $THEOS/sdks
          # 使用 iOS 15.5 SDK
          cp -r $THEOS/sdks-repo/iPhoneOS15.5.sdk $THEOS/sdks/
          ls -la $THEOS/sdks/

      - name: Build Package
        run: |
          export THEOS=$HOME/theos
          make package FINALPACKAGE=1
          ls -la packages/

      - name: Upload DEB Artifact
        uses: actions/upload-artifact@v4
        with:
          name: QianmianEnhancer-deb
          path: packages/*.deb
